#include <iostream>
#include <string>
using namespace std;

void test_case()
{

    int n;
    cin >> n;
    string enemy;
    cin >> enemy;
    string gregor;
    cin >> gregor;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--)
    {

        test_case();
    }
    return 0;
}